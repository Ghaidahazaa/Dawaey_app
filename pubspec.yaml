name: dawaey
description: تطبيق لتذكير المستخدم بتناول الأدوية.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^16.1.0
  intl: ^0.19.0
  timezone: ^0.9.4
  sqflite: ^2.2.8+4
  path: ^1.9.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # يمكنك إضافة خطوط أو صور مستقبلًا هنا:
  # assets:
  #   - assets/

  # fonts:
  #   - family: Cairo
  #     fonts:
  #       - asset: fonts/Cairo-Regular.ttf